var pathname = window.location.pathname; // Returns path only
var url      = window.location.href;     // Returns full URL
if(pathname=="/feedback-portal/"){
jQuery(document).ready(function() {
	jQuery('#demo').DataTable();    
} );
}
